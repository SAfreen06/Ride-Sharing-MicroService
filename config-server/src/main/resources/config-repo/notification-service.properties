server.port=8087

# Set MONGODB_URI locally as an env var (Atlas connection string) --
# never commit the real value, it contains DB credentials.
spring.data.mongodb.uri=${MONGODB_URI}
spring.mongodb.database=notification_db