server.port=8086

# Set MONGODB_URI locally as an env var (Atlas connection string) --
# never commit the real value, it contains DB credentials.
spring.data.mongodb.uri=${MONGODB_URI}
spring.mongodb.database=ride_matching_db

# jwt.secret comes from the shared application.properties (config-repo).