spring.application.name=config-server

server.port=8888

# Native backend: config files are served from src/main/resources/config-repo
# (simpler than standing up a separate git repo for a class project).
spring.profiles.active=native
spring.cloud.config.server.native.search-locations=classpath:/config-repo
