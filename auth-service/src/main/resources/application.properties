spring.application.name=auth-service

# Pull the rest of the config (port, datasource, eureka url, etc.)
# from the Config Server; keep running with local defaults if it's down.
spring.config.import=optional:configserver:http://localhost:8888
