spring.application.name=driver-service

# .env holds local secrets (MONGODB_URI) and is git-ignored -- never committed.
# Pull the rest of the config (port, eureka url, etc.) from the Config Server;
# keep running with local defaults if it's down.
spring.config.import=optional:file:.env[.properties],optional:configserver:http://localhost:8888
