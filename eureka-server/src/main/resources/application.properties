spring.application.name=eureka-server

server.port=8761

# This server is the registry itself, so it should not
# register with itself or try to fetch a registry from anywhere.
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false
